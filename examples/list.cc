#include <iostream>
#include <string>
#include <vector>
#include <map>
using namespace std;
#define print(a) cout << a << endl;


int main(){
vector<int> list = {1, 2, 3};
for (int i = 0; i < list.size(); i++) {
print(list[i]);
}
}
